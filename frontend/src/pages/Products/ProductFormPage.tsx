import { useEffect, useState } from 'react'
import type { FormEvent } from 'react'
import { Link, useNavigate, useSearchParams } from 'react-router-dom'
import { apiFetch } from '../../services/api'

type Product = Record<string, string | number | boolean | null | undefined> & { id: string }
const initialForm = { name: '', description: '', price: '', stock_quantity: '0', category: 'outros', product_type: 'resale', unit_of_measure: 'unit', cost_price: '', min_stock_quantity: '5', perishable: false, shelf_life_days: '', barcode: '', ncm_code: '', cest_code: '', fiscal_origin: '' }
const fields = [['name', 'Nome'], ['description', 'Descrição'], ['price', 'Preço de venda'], ['cost_price', 'Preço de custo'], ['stock_quantity', 'Estoque atual'], ['min_stock_quantity', 'Estoque mínimo'], ['shelf_life_days', 'Validade (dias)'], ['barcode', 'Código de barras'], ['ncm_code', 'NCM'], ['cest_code', 'CEST'], ['fiscal_origin', 'Origem fiscal (0 a 8)']] as const

export function ProductFormPage() {
  const navigate = useNavigate(); const [params] = useSearchParams(); const id = params.get('id'); const [form, setForm] = useState(initialForm); const [status, setStatus] = useState(''); const [loading, setLoading] = useState(Boolean(id))
  useEffect(() => { if (!id) return; apiFetch<Product[]>('/api/products').then((list) => { const product = list.find((item) => item.id === id); if (product) setForm(Object.fromEntries(Object.keys(initialForm).map((key) => [key, typeof product[key] === 'boolean' ? product[key] : product[key] == null ? initialForm[key as keyof typeof initialForm] : String(product[key])])) as typeof initialForm) }).catch((error) => setStatus(error instanceof Error ? error.message : 'Não foi possível carregar o produto.')).finally(() => setLoading(false)) }, [id])
  function update(field: keyof typeof initialForm, value: string | boolean) { setForm((current) => ({ ...current, [field]: value })) }
  function decimal(value: string) { return value ? Number(value.replace(',', '.')) : null }
  async function submit(event: FormEvent) { event.preventDefault(); setStatus(''); const price = decimal(form.price); if (!form.name.trim() || !price || price <= 0) { setStatus('Nome e preço válido são obrigatórios.'); return } if (form.perishable && !form.shelf_life_days) { setStatus('Informe a validade para produtos perecíveis.'); return } try { const payload = { ...form, price, stock_quantity: decimal(form.stock_quantity) || 0, cost_price: decimal(form.cost_price), min_stock_quantity: decimal(form.min_stock_quantity) || 5, shelf_life_days: form.perishable && form.shelf_life_days ? Number(form.shelf_life_days) : null, fiscal_origin: form.fiscal_origin ? Number(form.fiscal_origin) : null, description: form.description || null, barcode: form.barcode || null, ncm_code: form.ncm_code || null, cest_code: form.cest_code || null }; await apiFetch(id ? `/api/products/${id}` : '/api/products', { method: id ? 'PATCH' : 'POST', body: JSON.stringify(payload) }); navigate('/produtos') } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível salvar o produto.') } }
  if (loading) return <div className="page-wrap"><p className="table-status">Carregando produto...</p></div>
  return <div className="page-wrap"><header className="page-header"><div><p className="eyebrow">Cadastro manual</p><h1>{id ? 'Editar produto' : 'Novo produto'}</h1><p>Cadastre catálogo, estoque, custos, validade e informações fiscais.</p></div><Link className="secondary-button" to="/produtos">Voltar para produtos</Link></header><form className="page-card grid gap-6" onSubmit={submit}><div className="grid gap-4 md:grid-cols-2"><label className="form-label md:col-span-2">Nome<input className="form-input" value={form.name} onChange={(event) => update('name', event.target.value)} required /></label><label className="form-label md:col-span-2">Descrição<textarea className="form-input min-h-24" value={form.description} onChange={(event) => update('description', event.target.value)} /></label>{fields.slice(2, 6).map(([field, label]) => <label className="form-label" key={field}>{label}<input className="form-input" inputMode="decimal" value={form[field]} onChange={(event) => update(field, event.target.value)} /></label>)}</div><div className="grid gap-4 border-t border-line pt-5 md:grid-cols-3"><label className="form-label">Categoria<select className="form-input" value={form.category} onChange={(event) => update('category', event.target.value)}><option value="padaria">Padaria</option><option value="frios">Frios</option><option value="bebidas">Bebidas</option><option value="outros">Outros</option></select></label><label className="form-label">Tipo de produto<select className="form-input" value={form.product_type} onChange={(event) => update('product_type', event.target.value)}><option value="resale">Revenda</option><option value="manufactured">Fabricado</option></select></label><label className="form-label">Unidade<select className="form-input" value={form.unit_of_measure} onChange={(event) => update('unit_of_measure', event.target.value)}><option value="unit">Unidade</option><option value="kg">Quilograma</option><option value="g">Grama</option></select></label><label className="flex items-center gap-2 text-[13px] font-bold text-[#354064] md:col-span-3"><input type="checkbox" checked={form.perishable} onChange={(event) => update('perishable', event.target.checked)} /> Produto perecível</label>{form.perishable && <label className="form-label">Validade (dias)<input className="form-input" type="number" min="1" value={form.shelf_life_days} onChange={(event) => update('shelf_life_days', event.target.value)} required /></label>}</div><div className="grid gap-4 border-t border-line pt-5 md:grid-cols-3"><p className="eyebrow md:col-span-3">Identificação fiscal</p>{fields.slice(7).map(([field, label]) => <label className="form-label" key={field}>{label}<input className="form-input" value={form[field]} onChange={(event) => update(field, event.target.value)} /></label>)}</div>{status && <p className="form-status">{status}</p>}<div className="flex flex-wrap gap-3"><button className="primary-button" type="submit">Salvar produto</button><Link className="secondary-button" to="/produtos">Cancelar</Link></div></form></div>
}
